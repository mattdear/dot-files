" Enable line numbers
set number

" Enable column numbers
set ruler

" Enable syntax highlighting
syntax on

" Set tab size
set tabstop=4

" Enable auto indentation
set autoindent

" Enable replacing tabs with spaces
set expandtab

" Enable tab deletion with one backspace press
set softtabstop=4
